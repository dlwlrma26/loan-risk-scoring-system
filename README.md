# loan-risk-scoring-system
A production-style loan default risk scoring system featuring a full ML pipeline (EDA → preprocessing → feature engineering → model selection → calibration), cost-based threshold optimization, a FastAPI prediction service, a Streamlit analytics dashboard, and monitoring components for data drift (PSI/KS-test).
